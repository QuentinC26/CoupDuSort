services:
  back-end:
    build:
      context: ./back-end
      dockerfile: Dockerfile
    ports:
        - "8080:8080"
  front-end:
    build:
      context: ./front-end
      dockerfile: Dockerfile
    ports:
        - "4200:4200"
